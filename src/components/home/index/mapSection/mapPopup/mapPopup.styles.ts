import { TInlineStyle } from "@/types/style.types";

const popupContainerSx: TInlineStyle = () => ({
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
});

const mapPopupStyles = { popupContainerSx };
export default mapPopupStyles;
